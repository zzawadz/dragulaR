% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dragula.R
\name{dragula}
\alias{dragula}
\title{Register containers to dragula.}
\usage{
dragula(x, ...)
}
\arguments{
\item{x}{vector of containers ids. Their's elements will become draggable.}

\item{...}{additonal arguments passed to dragula JS as options. E.g. \code{id} will be an id to read from in shiny.
Additional shortcut options:
\itemize{
  \item \code{copyOnly}: container id from which elements can only be copied (not moved)
  \item \code{maxItems}: named list specifying maximum items per container, e.g. \code{maxItems = list(Model = 3)}
}}
}
\value{
Return htmlWidget. Should be used only inside shiny ui.
}
\description{
Create dragula instace to allow moving around elements of the registered containers.
}
\examples{

if(interactive()) {
  # Basic example
  path <- system.file("apps/example01-dragula", package = "dragulaR")
  runApp(path, display.mode = "showcase")

  # Example with maxItems (limits Model container to 3 items)
  path <- system.file("apps/example08-max-items", package = "dragulaR")
  runApp(path, display.mode = "showcase")
}

# Create dragula with maxItems limit
\dontrun{
dragula(c("Available", "Model"), maxItems = list(Model = 3))
}

}
